{
      "name"      : "app",
      "script"    : "server/index.js",
      "env"       : {
            "environment" : "{{ db_environment }}",
            "db_user"     : "{{ db_user }}",
            "db_password" : "{{ db_password }}"    
      }
}
