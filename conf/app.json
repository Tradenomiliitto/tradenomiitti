{
      "name"      : "app",
      "script"    : "server/index.js",
      "env"       : {
        "environment" : "{{ db_environment }}",
        "db_user"     : "{{ db_user }}",
        "db_host" : "{{ db_host }}",
        "db_password" : "{{ db_password }}",
        "db_name" : "{{ db_name }}",
        "COMMUNICATIONS_KEY": "{{ avoine_communications_key }}",
        "NON_LOCAL": "true",
        "TEST_LOGIN": "{{ test_login }}",
        "COOKIE_SECRET": "{{ cookie_secret }}",
        "DISABLE_SEBACON": "{{ disable_sebacon }}",
        "SEBACON_CUSTOMER": "{{ sebacon_customer }}",
        "SEBACON_USER": "{{ sebacon_user }}",
        "SEBACON_PASSWORD": "{{ sebacon_password }}",
        "SEBACON_AUTH": "{{ sebacon_auth }}",
        "ADMIN_GROUP": "{{ admin_group }}",
        "SMTP_HOST": "{{ smtp_host }}",
        "SMTP_USER": "{{ smtp_user }}",
        "SMTP_PASSWORD": "{{ smtp_password }}",
        "SMTP_TLS": "{{ smtp_tls }}",
        "MAIL_FROM": "{{ mail_from }}",
        "SERVICE_DOMAIN": "{{ service_domain }}",
        "ENABLE_EMAIL_SENDING": "{{ enable_emails }}",
        "RESTRICT_TO_GROUP": "{{ restrict_to_group }}"
      }
}
